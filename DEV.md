# DEV GUIDE

1. Commit and push your changes to remote
2. verify
3. git tag and push tag to remote

```shell
pod lib lint GCDWebsocketServer.podspec --allow-warnings
```

4. pod push

   ```shell
   pod trunk push GCDWebsocketServer.podspec --allow-warnings
   ```
